version: '3'

includes:
  infra:
    taskfile: ./infra.yml

tasks:

  up:
    cmds:
      - docker compose -f deploy/compose/compose.yml up -d
  
  down:
    cmds:
      - docker compose -f deploy/compose/compose.yml down
